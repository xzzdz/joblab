import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  experimental: {
    serverActions: {
      /**
       * เพดานขนาด body ของ Server Action (ค่าเริ่มต้นของ Next คือ 1 MB)
       *
       * ทำไมต้องแก้: ฟอร์มสมัครงานรับไฟล์ resume ได้ถึง 2 MB (ดู MAX_RESUME_BYTES
       * ใน src/lib/storage.ts) แต่ถ้าเพดานตรงนี้เป็น 1 MB ไฟล์ขนาด 1–2 MB
       * จะถูก Next ตัดทิ้งตั้งแต่ยังไม่ถึงโค้ดเรา ผู้ใช้เลยเห็น error ดิบ ๆ
       * แทนข้อความ "ไฟล์ใหญ่เกิน 2 MB" ที่เราตั้งใจเขียนไว้ — และป้ายบนฟอร์มก็จะโกหก
       *
       * ตั้ง 3 MB เผื่อส่วนหัวของ multipart และข้อความแนะนำตัวที่ส่งไปพร้อมกัน
       * ไม่ตั้งสูงกว่านี้เพราะ body ทั้งก้อนถูกอ่านเข้าหน่วยความจำก่อนประมวลผล
       * ยิ่งเพดานสูง ยิ่งเปิดช่องให้คนยิงไฟล์ใหญ่ ๆ ถล่มเซิร์ฟเวอร์ได้ง่ายขึ้น
       *
       * เจอปัญหานี้ตอนทดสอบ ไม่ใช่ตอนอ่านโค้ด — ไฟล์ 3 MB ควรถูกปฏิเสธด้วยข้อความของเรา
       * แต่กลับพังด้วย "Body exceeded 1 MB limit" ของ Next แทน
       */
      bodySizeLimit: "3mb",
    },
  },

  /**
   * HTTP header ด้านความปลอดภัย ใส่ให้ทุก response
   *
   * พวกนี้เป็นคำสั่งที่เราบอกเบราว์เซอร์ว่า "ห้ามทำอะไรบ้าง"
   * ไม่ได้ป้องกันช่องโหว่ในโค้ดเรา แต่ลดความเสียหายถ้ามีช่องโหว่หลุดไป
   */
  async headers() {
    return [
      {
        source: "/:path*",
        headers: [
          {
            // ห้ามเบราว์เซอร์เดาชนิดไฟล์เอง
            // ถ้าเราส่งไฟล์ที่ผู้ใช้อัปโหลดแล้วเบราว์เซอร์เดาว่าเป็น HTML มันจะรันสคริปต์ในนั้น
            key: "X-Content-Type-Options",
            value: "nosniff",
          },
          {
            // ห้ามเว็บอื่นเอาหน้าเราไปใส่ iframe ซ้อนทับ
            // กัน clickjacking — เทคนิคที่เอาหน้าเราไปซ่อนใต้ปุ่มปลอม
            // แล้วหลอกให้ผู้ใช้กดปุ่มจริงของเราโดยไม่รู้ตัว
            key: "X-Frame-Options",
            value: "DENY",
          },
          {
            // ส่งแค่ชื่อโดเมนไปกับ referrer ไม่ส่ง path เต็ม
            // ไม่งั้นเวลาผู้ใช้กดลิงก์เว็บบริษัทจากหน้าใบสมัคร
            // เว็บปลายทางจะเห็น URL ที่มี id ของใบสมัครติดไปด้วย
            key: "Referrer-Policy",
            value: "strict-origin-when-cross-origin",
          },
          {
            // ปิดสิทธิ์เข้าถึงอุปกรณ์ที่เว็บนี้ไม่ได้ใช้เลย
            // ถ้าวันหนึ่งมีสคริปต์แปลกปลอมหลุดเข้ามา มันก็ขอใช้กล้อง/ไมค์ไม่ได้
            key: "Permissions-Policy",
            value: "camera=(), microphone=(), geolocation=(), payment=()",
          },
        ],
      },
    ];
  },
};

export default nextConfig;
